# Deployments

This directory will contain deployment information for different networks.

After deployment, you will find files like:
- `sepolia.json` - Sepolia testnet deployment info
- `mainnet.json` - Mainnet deployment info (if deployed)

Each file contains:
- Contract address
- Deployer address
- Network information
- Chainlink VRF configuration
- Deployment timestamp
- Block number
